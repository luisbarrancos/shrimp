<shrimp type="function" name="Normalise" description="Normalise a vector">
	<input name = "A" description="Input" type="vector normal"/>
	<output name="val" description="val is a vector of unit length" type="vector normal"/>
	<code>
	$(val) = normalize( $(A) );
	</code>
	<usage>The output of this function is a vector with the same direction as the input but a length of 1.
	</usage>
</shrimp>