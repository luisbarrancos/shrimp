<shrimp type="function" name="atan" description="Arc tangent block">
	<input name="a" description="Input value"/>
	<output name="val" description="val = atan( a )"/>
	<code>
	$(val) = atan( $(a) );
	</code>
	<usage>Returns the arc tangent in the range -Pi/2 to +PI/2
	</usage>
</shrimp>