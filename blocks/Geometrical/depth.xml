<shrimp type="function" name="Depth" description="Depth of point p." author="rconstruct">
	<input name = "p" description="point to test" type="point" default="P"/>
	<output name="out" description="The depth of point p in Camera co-ordinates" type="float"/>
	<rsl_code>
	$(out) = depth( $(p) );
	</rsl_code>
<usage>Returns the depth of the point P in normalised camera coordinates. The depth is normalized so that 0 is at the near clipping plane, and 1 at the far clipping plane.</usage>
</shrimp>
