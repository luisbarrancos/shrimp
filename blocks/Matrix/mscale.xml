<shrimp type="function" name="Mscale" description="Scales matrix M" author="rconstruct">
    <input name="minput" description="Matrix input" type="matrix" default="1"/>
	<input name="inpoint" description="Point input" type="point" default="P"/>
    <output name="mout" description="Translated matrix" type="matrix"/>
	<rsl_code>
	$(mout) = scale( $(minput), $(inpoint) );
	</rsl_code>
	<usage>Scales a matrix M. These functions are similar to the RI functions RiTranslate, RiRotate, and RiScale, except that the rotation angle in rotate() is in radians, not in degrees as with RiRotate.</usage>
</shrimp>
