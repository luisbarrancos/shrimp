<shrimp type="function" name="SmoothStep" description="Smooth step function" author="rconstruct">
	<input name="x" description="value to test" type="float" default="0"/>
	<input name="min" description="position of bottom of step" type="float" default="0"/>
	<input name="max" description="position of top of step" type="float" default="1"/>
	<output name="val" description="value of smoothstep function at x" type="float"/>
	<rsl_code>
		$(val) = smoothstep( $(min), $(max), $(x) );
	</rsl_code>
	<usage>val is zero if x is less than min, one if x is greater than max, otherwise it is interpolated between zero and one with a smooth hermite curve.
	</usage>
</shrimp>
