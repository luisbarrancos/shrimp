<shrimp type="function" name="SlideProjLight" description="Slide projector light shader" author="rconstruct">
	<input name="inten" description="Light intensity" type="float" default="1"/>
	<input name="lcolor" description="Light color" type="color" default="color (1, 1, 1)"/>
	<input name="fov" description="Field of view" type="float" default="45"/>
	<input name="from" description="Light source position" type="point" default="point ( -15, 10, 5 )"/>
	<input name="to" description="Light source target" type="point" default="point( 0, 0, 0 )"/>
	<input name="up" description="Up position" type="point" default="point &quot;eye&quot; ( 0, 1, 0 )"/>
	<input name="slidename" description="Slide map name" type="string" default="&quot;&quot;"/>
	<input name="shadowname" description="Shadow map name" type="string" default="&quot;&quot;"/>
	<input name="shadowfilter" description="Shadow map filter" type="string" default="&quot;gaussian&quot;"/>
	<input name="shadowblur" description="Shadow map blur" type="float" default="0"/>
	<input name="shadowsamples" description="Shadow map samples" type="float" default="1"/>
	<input name="shadowwidth" description="Shadow map filter width" type="float" default="1"/>
	<input name="shadowbias" description="Shadow map bias" type="float" default="-1"/>
	<output name="Cl" description="Light ray color" type="color"/>
	<include>lights.h</include>
	<code>
		$(Cl) = $(inten) * $(lcolor) * slideprojector( radians( $(fov)),
							$(from), $(to), $(up), $(slidename),
							$(shadowname), $(shadowfilter), $(shadowblur),
							$(shadowsamples), $(shadowwidth), $(shadowbias) );
	</code>
<usage>Slide projector light shader, from The RenderMan Repository, www.renderman.org . Requires a texture map (slide map) to work. Shadow bias defaults to global default, -1, while shadow filter type defaults to gaussian, with triangle and box as optional.</usage>
</shrimp>
