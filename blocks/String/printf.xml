<shrimp type="function" name="Printf" description="Prints values of variables" author="rconstruct">
	<input name="str" description="String to use, see help" storage="uniform" type="string" default="&quot;Content of var =&quot;"/>
	<input name="var" description="Variable input" type="float color normal vector point matrix string" default="0"/>
	<output name="val" description="Variable output" type="float color normal vector point matrix string"/>
	<output name="status" description="Status" type="float" />
	<code>
	#define var_$(var:type)
	#ifdef var_float
		printf( concat( $(str), " %f\n"), $(var) );
	#endif
	#ifdef var_color
		printf( concat( $(str), " %c\n"), $(var) );
	#endif
	#ifdef var_matrix
		printf( concat( $(str), " %8m\n"), $(var) );
	#endif
	#ifdef var_string
		printf( concat( $(str), " %s\n"), $(var) );
	#else
		printf( concat( $(str), " %p\n"), $(var) );
	#endif
	
		$(val) = $(var);
		$(status) = 1;
	</code>
	<usage>Similar to the C printf, prints the contents of the specified variables into stdout. The formatting for the string uses %f, %c, %p, %m and %s, for variables of type float, color, normal/vector/point, matrix, and string.</usage>
</shrimp>
