<shrimp type="function" name="Match" description="Searches subject string for pattern string" author="rconstruct">
	<input name="stra" description="Pattern string" type="string" default="&quot;&quot;"/>
	<input name="strb" description="String to search" type="string" default="&quot;&quot;"/>
	<output name="status" description="Return status" type="float"/>
	<code>
		$(status) = match( $(stra), $(strb) );
	</code>
	<usage>Searches subject string, for pattern string, and returns 1.0 if the pattern exists in the subject, if not, it returns 0.0. The pattern can be any regex expression (man regex for more information).</usage>
</shrimp>
