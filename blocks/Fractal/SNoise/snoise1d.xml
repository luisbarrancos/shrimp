<shrimp type="function" name="SNoise1D" description="1D signed perlin noise" author="rconstruct">
	<input name="x" description="value to sample noise at" type="float" default="s"/>
	<input name="amplitude" description="Amplitude of noise" type="float" default="1"/>
	<output name="val" description="Noise value at x, in range -1 - 1" type="float color point vector"/>
	<include>fractal.h</include>
	<rsl_code>
	#define $(val)_$(val:type)
	#ifdef $(val)_float
		$(val) = $(amplitude) * snoise( $(x) );
	#else
		$(val) = $(amplitude) * $(val:type) vsnoise( $(x) );
	#endif
	</rsl_code>
	<usage>SNoise is exactly like Noise except that the result ranges from -1 to 1 rather than 0 to 1. The average value is therefore zero.
	</usage> 
</shrimp>
