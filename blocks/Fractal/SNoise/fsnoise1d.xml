<shrimp type="function" name="FSNoise1D" description="1D filtered signed perlin noise" author="rconstruct">
	<input name="x" description="value to sample noise at" type="float" default="s"/>
	<input name="width" description="filter width" type="float" default="0"/>
	<input name="amplitude" description="Amplitude of noise" type="float" default="1"/>
	<output name="val" description="Noise value at x, in range -1 - 1" type="float color point vector"/>
	<include>shrimp_fractal.h</include>
	<rsl_code>
	#define $(val)_$(val:type)
	#ifdef $(val)_float
		$(val) = $(amplitude) * filteredsnoise( $(x), $(width) );
	#else
		$(val) = $(amplitude) * $(val:type) filteredvsnoise( $(x), $(width) );
	#endif
	</rsl_code>
	<usage>Filter version of signed noise. SNoise is exactly like Noise except that the result ranges from -1 to 1 rather than 0 to 1. The average value is therefore zero.
	</usage> 
</shrimp>
