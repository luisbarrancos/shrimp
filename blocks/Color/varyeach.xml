<shrimp type="function" name="VaryEach" description="Vary Each by Larry Gritz" author="rconstruct">
	<input name="cin" description="Input color" type="color" default="0.5"/>
	<input name="index" description="Index" type="float" default="1"/>
    <input name="varyhue" description="Hue variation" type="float" default="1"/>
    <input name="varysat" description="Saturation variation" type="float" default="1"/>
    <input name="varylum" description="Luminance variation" type="float" default="1"/>
	<output name="out" description="The resulting color" type="color"/>
    <include>shrimp_helpers.h</include>
	<code>
		$(out) = varyEach( $(cin), $(index), $(varyhue), $(varysat), $(varylum) );
	</code>
<usage>VaryEach by Larry Gritz, takes a computed color, then tweaks each indexed item separately to add some variation. Hue, saturation and lightness are all independently controlled. Hue adds, but saturation and lightness multiply.</usage>
</shrimp>
