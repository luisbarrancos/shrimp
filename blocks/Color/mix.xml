<shrimp type="function" name="Mix" description="Performs a linear blend between A and B" author="rconstruct">
	<input name="alpha" description="The mixing factor" type="color normal point vector float" default="0.5"/>
	<input name="A" description="1st Input" type_parent="val" type="color normal point vector float" default="0"/>
	<input name="B" description="2nd Input" type_parent="val" type="color normal point vector float" default="1"/>
	<output name="val" description="The resulting mix between A and B" type="color normal point vector float"/>
	<rsl_code>
		$(val) = mix( $(A), $(B), $(alpha) );
	</rsl_code>
<usage>Mix returns x*(1-a)+y*a, that is, it performs a linear blend between values x and y. The types of x and y must be identical, but may be any of float, point, vector, normal, or color. The variants that operate on colors or point-like objects operate on a component by component basis (e.g: separately for x, y, and z).</usage>
</shrimp>
